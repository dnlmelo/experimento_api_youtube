# Experience using api v3 youtube

This project is an experience application using the youtube api v3 with angularJs


### Install Dependencies

```
npm install
```

### Run the Application

```
npm start
```

Now browse to the app at `http://localhost:8000/app/index.html`.

